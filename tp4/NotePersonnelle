# Utilisez une compréhension de liste pour générer les résultats de multiplication
  results = [round((multiplied * x), 1) for x in range(0, 10)]
  variable = [operation voulue en fonction de x] for x in [liste]
